@{
  Layout = "_Layout";
}

@model Registrar.Models.Course

<h2>Add a student to this sad a#* course :C</h2>
<h4>Add student to this course: @Html.DisplayFor(model => model.Name)</h4>

@using (Html.BeginForm())
{
  @Html.HiddenFor(model => model.CourseId)
  @Html.Label("Select a Student")
  @Html.DropDownList("StudentId")
  
  <input type="submit" value="Save" />
}

<div>
  <div class="outside_side_bar">
    <div class="side_bar">
      <header>Epic Navi:</header>
      <br>
      <span>@Html.ActionLink("Back to Course", "Index")</span>
      <br>
      <span>@Html.ActionLink("Home", "Index", "Home")</span>
    </div>
  </div>
</div>
<br>